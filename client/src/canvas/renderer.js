function stroke(ctx, object) { const { path = [] } = object; if (!path.length) return; ctx.beginPath(); ctx.moveTo(path[0].x, path[0].y); path.slice(1).forEach((point) => ctx.lineTo(point.x, point.y)); ctx.stroke(); }
function shape(ctx, object) { ctx.beginPath(); if (object.type === 'rect') ctx.rect(object.x, object.y, object.w, object.h); else if (object.type === 'circle') ctx.arc(object.x, object.y, object.r, 0, Math.PI * 2); else if (object.type === 'line' || object.type === 'arrow') { ctx.moveTo(object.x1, object.y1); ctx.lineTo(object.x2, object.y2); if (object.type === 'arrow') { const a = Math.atan2(object.y2-object.y1, object.x2-object.x1); ctx.lineTo(object.x2-15*Math.cos(a-Math.PI/6), object.y2-15*Math.sin(a-Math.PI/6)); ctx.moveTo(object.x2, object.y2); ctx.lineTo(object.x2-15*Math.cos(a+Math.PI/6), object.y2-15*Math.sin(a+Math.PI/6)); } } else if (object.type === 'text') { ctx.font = `${object.fontSize || 16}px sans-serif`; ctx.fillText(object.text || '', object.x, object.y); return; } else return; if (object.fill && object.fill !== 'transparent') ctx.fill(); ctx.stroke(); }
export function drawObject(ctx, object) { ctx.save(); ctx.lineJoin = 'round'; ctx.lineCap = 'round'; ctx.strokeStyle = object.color || '#000'; ctx.fillStyle = object.fill || object.color || '#000'; ctx.lineWidth = object.width || 2; if (object.style === 'dashed') ctx.setLineDash([10,6]); else if (object.style === 'dotted') ctx.setLineDash([2,6]); if (object.path) stroke(ctx, object); else shape(ctx, object); ctx.restore(); }
export function renderDocument(canvas, document) { const ctx = canvas.getContext('2d'); ctx.clearRect(0, 0, canvas.width, canvas.height); document.objects.forEach((object) => drawObject(ctx, object)); }
